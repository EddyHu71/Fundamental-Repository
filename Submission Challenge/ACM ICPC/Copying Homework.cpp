#include <iostream>
#include <math>
using namespace std;
main () {
	int N;
	cin>>N;
	int a[N];
	for (int i=0;i<N;i++) {
		cin>>a[i];
	}
	int init = 0;
	int akhir = N;
	for (init < N && akhir > 0) {
		res = abs)
	}
}
