#include <iostream>
using namespace std;
main () {
    int T, n, p, k;
    while (T--) {
        cin>>n>>p>>k;
        int a[p*n], b[p];
        for (int i=0;i<p*n;i++) {
            cin>>a[i];
        }
        
    }
}
